#!/usr/bin/python3
import add_0
if __name__ == "__main__":  # makes sure the code is not executed when imported
    a = 1
    b = 2
    print(f"{a} + {b} = {add_0.add(a, b)}")  # prints the sum of a and b
